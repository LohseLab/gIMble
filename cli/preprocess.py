"""
usage: gimble preprocess                 [-h|--help]
                                            
        -h, --help                       Install gimble-preprocess instead
"""

import sys
import os
from docopt import docopt

def main(params):
    sys.exit("[X] gimble preprocess has been replaced by gimbleprep available on bioconda.")
